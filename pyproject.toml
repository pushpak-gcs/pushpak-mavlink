[project]
name = "pushpak-mavlink"
version = "0.1.0"
description = "MAVLink logic for Pushpak GCS"
authors = [
    { name = "Pushpak GCS", email = "ranapoddarmath2@gmail.com" }
]
dependencies = [
    "pymavlink",
]
readme = "README.md"
requires-python = ">= 3.8"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
